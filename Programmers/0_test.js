/*function meetAt(yyyy,mm,dd){
    if(yyyy && !mm && !dd){
        return yyyy+"년"
    }else if(yyyy && mm && !dd){
        return yyyy+"년 "+mm+"월"
    }else if(yyyy && mm && dd){
        return yyyy+"/"+mm+"/"+dd
    }
}
meetAt(2022)*/

function findSmallestElement(n){
    let minValue
    for(let i=0;i<n.length;i++){}
    return minValue
}

let arr = [100,200,3,0,2,1];
findSmallestElement(arr)